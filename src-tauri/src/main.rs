// ============================================================
// OpenCat Tauri 桌面端 — Rust 入口
// ============================================================
// Tauri 2.0 使用 lib.rs 作为核心入口
// main.rs 只是启动入口，实际逻辑在 lib.rs

fn main() {
    opencat_lib::run();
}
